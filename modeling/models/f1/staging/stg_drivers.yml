# schema.yml

version: 2

models:
  - name: stg_drivers
    columns:
      - name: driver_id
        tests:
          - not_null
          - unique
      - name: driver_ref
        tests:
          - not_null
